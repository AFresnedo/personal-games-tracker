<h1>Wish List</h1>
<% if (titles) { %>
  <% titles.forEach(function(title) { %>

    <p><%= title %></p>
    <% if (currentUser.id == ownerId) { %>
      <button id='del-wish'>Delete</button>
    <% } %>
    <hr>
  <% }); %>
  <% } else {%>
    Wish list is empty.
  <% } %>
